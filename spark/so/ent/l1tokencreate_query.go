// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/so/ent/l1tokencreate"
	"github.com/lightsparkdev/spark/so/ent/predicate"
)

// L1TokenCreateQuery is the builder for querying L1TokenCreate entities.
type L1TokenCreateQuery struct {
	config
	ctx        *QueryContext
	order      []l1tokencreate.OrderOption
	inters     []Interceptor
	predicates []predicate.L1TokenCreate
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the L1TokenCreateQuery builder.
func (lcq *L1TokenCreateQuery) Where(ps ...predicate.L1TokenCreate) *L1TokenCreateQuery {
	lcq.predicates = append(lcq.predicates, ps...)
	return lcq
}

// Limit the number of records to be returned by this query.
func (lcq *L1TokenCreateQuery) Limit(limit int) *L1TokenCreateQuery {
	lcq.ctx.Limit = &limit
	return lcq
}

// Offset to start from.
func (lcq *L1TokenCreateQuery) Offset(offset int) *L1TokenCreateQuery {
	lcq.ctx.Offset = &offset
	return lcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (lcq *L1TokenCreateQuery) Unique(unique bool) *L1TokenCreateQuery {
	lcq.ctx.Unique = &unique
	return lcq
}

// Order specifies how the records should be ordered.
func (lcq *L1TokenCreateQuery) Order(o ...l1tokencreate.OrderOption) *L1TokenCreateQuery {
	lcq.order = append(lcq.order, o...)
	return lcq
}

// First returns the first L1TokenCreate entity from the query.
// Returns a *NotFoundError when no L1TokenCreate was found.
func (lcq *L1TokenCreateQuery) First(ctx context.Context) (*L1TokenCreate, error) {
	nodes, err := lcq.Limit(1).All(setContextOp(ctx, lcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{l1tokencreate.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (lcq *L1TokenCreateQuery) FirstX(ctx context.Context) *L1TokenCreate {
	node, err := lcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first L1TokenCreate ID from the query.
// Returns a *NotFoundError when no L1TokenCreate ID was found.
func (lcq *L1TokenCreateQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = lcq.Limit(1).IDs(setContextOp(ctx, lcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{l1tokencreate.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (lcq *L1TokenCreateQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := lcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single L1TokenCreate entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one L1TokenCreate entity is found.
// Returns a *NotFoundError when no L1TokenCreate entities are found.
func (lcq *L1TokenCreateQuery) Only(ctx context.Context) (*L1TokenCreate, error) {
	nodes, err := lcq.Limit(2).All(setContextOp(ctx, lcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{l1tokencreate.Label}
	default:
		return nil, &NotSingularError{l1tokencreate.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (lcq *L1TokenCreateQuery) OnlyX(ctx context.Context) *L1TokenCreate {
	node, err := lcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only L1TokenCreate ID in the query.
// Returns a *NotSingularError when more than one L1TokenCreate ID is found.
// Returns a *NotFoundError when no entities are found.
func (lcq *L1TokenCreateQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = lcq.Limit(2).IDs(setContextOp(ctx, lcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{l1tokencreate.Label}
	default:
		err = &NotSingularError{l1tokencreate.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (lcq *L1TokenCreateQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := lcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of L1TokenCreates.
func (lcq *L1TokenCreateQuery) All(ctx context.Context) ([]*L1TokenCreate, error) {
	ctx = setContextOp(ctx, lcq.ctx, ent.OpQueryAll)
	if err := lcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*L1TokenCreate, *L1TokenCreateQuery]()
	return withInterceptors[[]*L1TokenCreate](ctx, lcq, qr, lcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (lcq *L1TokenCreateQuery) AllX(ctx context.Context) []*L1TokenCreate {
	nodes, err := lcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of L1TokenCreate IDs.
func (lcq *L1TokenCreateQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if lcq.ctx.Unique == nil && lcq.path != nil {
		lcq.Unique(true)
	}
	ctx = setContextOp(ctx, lcq.ctx, ent.OpQueryIDs)
	if err = lcq.Select(l1tokencreate.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (lcq *L1TokenCreateQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := lcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (lcq *L1TokenCreateQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, lcq.ctx, ent.OpQueryCount)
	if err := lcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, lcq, querierCount[*L1TokenCreateQuery](), lcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (lcq *L1TokenCreateQuery) CountX(ctx context.Context) int {
	count, err := lcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (lcq *L1TokenCreateQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, lcq.ctx, ent.OpQueryExist)
	switch _, err := lcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (lcq *L1TokenCreateQuery) ExistX(ctx context.Context) bool {
	exist, err := lcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the L1TokenCreateQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (lcq *L1TokenCreateQuery) Clone() *L1TokenCreateQuery {
	if lcq == nil {
		return nil
	}
	return &L1TokenCreateQuery{
		config:     lcq.config,
		ctx:        lcq.ctx.Clone(),
		order:      append([]l1tokencreate.OrderOption{}, lcq.order...),
		inters:     append([]Interceptor{}, lcq.inters...),
		predicates: append([]predicate.L1TokenCreate{}, lcq.predicates...),
		// clone intermediate query.
		sql:       lcq.sql.Clone(),
		path:      lcq.path,
		modifiers: append([]func(*sql.Selector){}, lcq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.L1TokenCreate.Query().
//		GroupBy(l1tokencreate.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (lcq *L1TokenCreateQuery) GroupBy(field string, fields ...string) *L1TokenCreateGroupBy {
	lcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &L1TokenCreateGroupBy{build: lcq}
	grbuild.flds = &lcq.ctx.Fields
	grbuild.label = l1tokencreate.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.L1TokenCreate.Query().
//		Select(l1tokencreate.FieldCreateTime).
//		Scan(ctx, &v)
func (lcq *L1TokenCreateQuery) Select(fields ...string) *L1TokenCreateSelect {
	lcq.ctx.Fields = append(lcq.ctx.Fields, fields...)
	sbuild := &L1TokenCreateSelect{L1TokenCreateQuery: lcq}
	sbuild.label = l1tokencreate.Label
	sbuild.flds, sbuild.scan = &lcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a L1TokenCreateSelect configured with the given aggregations.
func (lcq *L1TokenCreateQuery) Aggregate(fns ...AggregateFunc) *L1TokenCreateSelect {
	return lcq.Select().Aggregate(fns...)
}

func (lcq *L1TokenCreateQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range lcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, lcq); err != nil {
				return err
			}
		}
	}
	for _, f := range lcq.ctx.Fields {
		if !l1tokencreate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if lcq.path != nil {
		prev, err := lcq.path(ctx)
		if err != nil {
			return err
		}
		lcq.sql = prev
	}
	return nil
}

func (lcq *L1TokenCreateQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*L1TokenCreate, error) {
	var (
		nodes = []*L1TokenCreate{}
		_spec = lcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*L1TokenCreate).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &L1TokenCreate{config: lcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(lcq.modifiers) > 0 {
		_spec.Modifiers = lcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, lcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (lcq *L1TokenCreateQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := lcq.querySpec()
	if len(lcq.modifiers) > 0 {
		_spec.Modifiers = lcq.modifiers
	}
	_spec.Node.Columns = lcq.ctx.Fields
	if len(lcq.ctx.Fields) > 0 {
		_spec.Unique = lcq.ctx.Unique != nil && *lcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, lcq.driver, _spec)
}

func (lcq *L1TokenCreateQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(l1tokencreate.Table, l1tokencreate.Columns, sqlgraph.NewFieldSpec(l1tokencreate.FieldID, field.TypeUUID))
	_spec.From = lcq.sql
	if unique := lcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if lcq.path != nil {
		_spec.Unique = true
	}
	if fields := lcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, l1tokencreate.FieldID)
		for i := range fields {
			if fields[i] != l1tokencreate.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := lcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := lcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := lcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := lcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (lcq *L1TokenCreateQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(lcq.driver.Dialect())
	t1 := builder.Table(l1tokencreate.Table)
	columns := lcq.ctx.Fields
	if len(columns) == 0 {
		columns = l1tokencreate.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if lcq.sql != nil {
		selector = lcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if lcq.ctx.Unique != nil && *lcq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range lcq.modifiers {
		m(selector)
	}
	for _, p := range lcq.predicates {
		p(selector)
	}
	for _, p := range lcq.order {
		p(selector)
	}
	if offset := lcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := lcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (lcq *L1TokenCreateQuery) ForUpdate(opts ...sql.LockOption) *L1TokenCreateQuery {
	if lcq.driver.Dialect() == dialect.Postgres {
		lcq.Unique(false)
	}
	lcq.modifiers = append(lcq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return lcq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (lcq *L1TokenCreateQuery) ForShare(opts ...sql.LockOption) *L1TokenCreateQuery {
	if lcq.driver.Dialect() == dialect.Postgres {
		lcq.Unique(false)
	}
	lcq.modifiers = append(lcq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return lcq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (lcq *L1TokenCreateQuery) Modify(modifiers ...func(s *sql.Selector)) *L1TokenCreateSelect {
	lcq.modifiers = append(lcq.modifiers, modifiers...)
	return lcq.Select()
}

// L1TokenCreateGroupBy is the group-by builder for L1TokenCreate entities.
type L1TokenCreateGroupBy struct {
	selector
	build *L1TokenCreateQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lcgb *L1TokenCreateGroupBy) Aggregate(fns ...AggregateFunc) *L1TokenCreateGroupBy {
	lcgb.fns = append(lcgb.fns, fns...)
	return lcgb
}

// Scan applies the selector query and scans the result into the given value.
func (lcgb *L1TokenCreateGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lcgb.build.ctx, ent.OpQueryGroupBy)
	if err := lcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*L1TokenCreateQuery, *L1TokenCreateGroupBy](ctx, lcgb.build, lcgb, lcgb.build.inters, v)
}

func (lcgb *L1TokenCreateGroupBy) sqlScan(ctx context.Context, root *L1TokenCreateQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lcgb.fns))
	for _, fn := range lcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lcgb.flds)+len(lcgb.fns))
		for _, f := range *lcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// L1TokenCreateSelect is the builder for selecting fields of L1TokenCreate entities.
type L1TokenCreateSelect struct {
	*L1TokenCreateQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lcs *L1TokenCreateSelect) Aggregate(fns ...AggregateFunc) *L1TokenCreateSelect {
	lcs.fns = append(lcs.fns, fns...)
	return lcs
}

// Scan applies the selector query and scans the result into the given value.
func (lcs *L1TokenCreateSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lcs.ctx, ent.OpQuerySelect)
	if err := lcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*L1TokenCreateQuery, *L1TokenCreateSelect](ctx, lcs.L1TokenCreateQuery, lcs, lcs.inters, v)
}

func (lcs *L1TokenCreateSelect) sqlScan(ctx context.Context, root *L1TokenCreateQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(lcs.fns))
	for _, fn := range lcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*lcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (lcs *L1TokenCreateSelect) Modify(modifiers ...func(s *sql.Selector)) *L1TokenCreateSelect {
	lcs.modifiers = append(lcs.modifiers, modifiers...)
	return lcs
}
